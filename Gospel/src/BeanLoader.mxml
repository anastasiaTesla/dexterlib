<?xml version="1.0" encoding="utf-8"?>
<DexterBeanLoader xmlns:fx="http://ns.adobe.com/mxml/2009" xmlns="*"
		 xmlns:s="library://ns.adobe.com/flex/spark" 
		 xmlns:mx="library://ns.adobe.com/flex/halo" xmlns:controllers="controllers.*" xmlns:models="models.*">
	<fx:Declarations>
		<models:GospelModel id="gospelModel"/>
		<models:LocalSetting id="localSetting"/>
		<models:StratusConnection id="stratus"/>
		<models:ServerConnection id="server"/>
		<models:SOProxy id="proxy"/>
		<models:InStreamProxy id="inStreamProxy"/>
		<models:OutStreamProxy id="outStreamProxy"/>
		<models:GroupSpecifierProxy id="groupSpecifierProxy"/>
		<controllers:ApplicationController id="applicationController"/>
		<controllers:ConnectionController id="connectionController"/>
		<controllers:ChatController id="chatController"/>
		<controllers:VideoController id="videoController"/>
	</fx:Declarations>
</DexterBeanLoader>
